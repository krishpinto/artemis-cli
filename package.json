{
  "name": "artemis-cli",
  "version": "1.0.0",
  "description": "One-command local dev infrastructure for Kubernetes",
  "type": "module",
  "main": "dist/cli.js",
  "bin": {
    "artemis": "./dist/cli.js"
  },
  "scripts": {
    "build": "node build.js",
    "dev": "npm run build && node dist/cli.js",
    "status": "npm run build && node dist/cli.js status",
    "down": "npm run build && node dist/cli.js down"
  },
  "keywords": ["kubernetes", "cli", "devtools"],
  "author": "Krish Pinto",
  "license": "MIT",
  "dependencies": {
    "@kubernetes/client-node": "^1.4.0",
    "chalk": "^5.6.2",
    "ink": "^7.0.0",
    "ink-spinner": "^5.0.0"
  },
  "devDependencies": {
    "esbuild": "^0.28.0"
  }
}
